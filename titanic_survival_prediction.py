# -*- coding: utf-8 -*-
"""Titanic survival prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/18cDswz5laH8ZCl_tCxR5CMX4YYJrp6k5
"""

import pandas as pd
import numpy as np
import seaborn as sns
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.preprocessing import LabelEncoder
from sklearn.metrics import accuracy_score

data =pd.read_csv("Titanic-Dataset.csv")
data.head()

data['Age'].fillna(data['Age'].mean(), inplace=True)
data['Embarked'].fillna(data['Embarked'].mode()[0], inplace=True)
data.drop(['Cabin', 'Name', 'Ticket', 'PassengerId'], axis=1, inplace=True)

le = LabelEncoder()
data['Sex'] = le.fit_transform(data['Sex'])
data['Embarked'] = le.fit_transform(data['Embarked'])

X = data.drop('Survived', axis=1)
y = data['Survived']

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)

model = LogisticRegression(max_iter=200)
model.fit(X_train, y_train)

y_pred = model.predict(X_test)
print("Accuracy:", accuracy_score(y_test, y_pred))

sns.countplot(x='Survived', data=data)
plt.show()

sns.countplot(x='Sex', hue='Survived', data=data)
plt.show()

sns.countplot(x='Pclass', hue='Survived', data=data)
plt.show()

"""Logistic Regression is used for classification problems.

It predicts whether a passenger survived (1) or not (0).

The model learns patterns from features like age, gender, class.

Conclusion


The Titanic survival prediction model was successfully developed using logistic regression. After preprocessing the dataset and training the model, an accuracy of approximately 83% was achieved. The analysis shows that factors like gender, passenger class, and age significantly influenced survival chances. This project demonstrates the effectiveness of machine learning in predicting real-world outcomes.
"""